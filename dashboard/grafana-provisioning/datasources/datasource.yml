apiVersion: 1

datasources:
  - name: Air Quality DB
    type: postgres
    uid: air_quality_db
    access: proxy
    isDefault: true
    url: postgres:5432
    user: airquality
    secureJsonData:
      password: airquality123
    jsonData:
      database: air_quality
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    editable: true
